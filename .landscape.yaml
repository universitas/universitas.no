# doc-warnings: true
# test-warnings: false
strictness: medium
pep8:
  full: true
  disable:
    - E1004
pylint:
  disable:
    - dangerous-default-value
    - missing-super-argument
max-line-length: 120
uses:
  - django
# autodetect: yes
# requirements:
  # - requirements.txt
# ignore-paths:
ignore-patterns:
  - /migrations/