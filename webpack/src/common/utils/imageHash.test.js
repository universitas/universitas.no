// there should be tests here, but I could not get canvas to work with jest / jsdom
import { imageFingerPrint } from 'utils/imageHash'

test('dummy', () => expect(null).toEqual(null)) // dummy test to stop jest from complaining
