# vim: ft=systemd
# This file, when installed, configures the ubuntu web server to start all
# all required docker containers on startup.

[Unit]
Description=universitas.no website
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/bin/bash -c "cd /srv/tassen; ./run prod dc up"
ExecStop=/bin/bash -c "cd /srv/tassen; ./run prod dc down"
ExecReload=/bin/bash -c "cd /srv/tassen; ./run prod static expressb dc up -d --no-deps"

[Install]
WantedBy=multi-user.target
