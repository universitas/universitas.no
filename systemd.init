[Unit]
Description=universitas.no website
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/bin/bash -c "cd /srv/tassen; ./run prod up"
ExecStop=/bin/bash -c "cd /srv/tassen; docker-compose down"

[Install]
WantedBy=local.target
